# Continuation FormatStandard format for presenting next steps after completing a command or workflow.## Core Structure```html```csv
```html```bash
```html```bash
```html```bash
```csv```bash
```csv```bash
```css```bash
```markdown### Phase 2: Authentication**Goal**: JWT login flow with refresh tokens```css```markdownPlans:- [ ] 02-03: Add refresh token rotation```text```xml<objective>Add refresh token rotation with sliding expiry window.Purpose: Extend session lifetime without compromising security.</objective>```css```bash
```css```text
```css```markdown
```css```text
```text```text
```Fenced blocks inside templates create nesting ambiguity. Use inline backticks instead.