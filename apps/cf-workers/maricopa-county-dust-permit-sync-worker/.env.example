# This worker does not require runtime environment variables.
# Optional: use these only with the `deploy:local` or
# `d1:migrate:remote:local` scripts if you want to load Cloudflare CLI auth
# from a local `.env` file instead of your shell or CI.

CLOUDFLARE_API_TOKEN=your_cloudflare_api_token
