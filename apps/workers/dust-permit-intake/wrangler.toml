name = "dust-permit-intake"
main = "src/index.ts"
compatibility_date = "2026-02-06"
compatibility_flags = ["nodejs_compat_v2"]

send_email = [
  { name = "SEND_EMAIL", destination_address = "chi@desertservices.net" }
]

[observability]
enabled = true

# DEPRECATED: dustpermits@ routing moved to intake-worker.
# Keep only as emergency fallback during migration window.
# Workers.dev URL: dust-permit-intake.cheez2012.workers.dev
#
# Secrets (set via `wrangler secret put`):
# - HUB_WEBHOOK_URL (https://monday-estimates.desertservices.app)
