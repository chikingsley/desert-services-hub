# Architecture Redirect

This path is intentionally a redirect to avoid overlapping architecture sources.

Use these canonical docs instead:
- `REPO_ORGANIZATION.md` for repository structure, ownership, and standards
- `SYSTEM-MAP.md` for runtime topology, active gaps, and execution backlog

Historical architecture snapshot:
- `docs/_archive/reference/architecture/ARCHITECTURE-legacy.md`
