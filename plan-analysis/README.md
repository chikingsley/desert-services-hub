# Moved: Plan Analysis

`plan-analysis` has been consolidated into `apps/pdf-analysis`.

The active Python package, providers, and CLI now live only in:

- `apps/pdf-analysis`

## Equivalent Commands

```bash
# Old
# cd plan-analysis && just ocr /path/to/file.pdf

# New
cd apps/pdf-analysis
just ocr /path/to/file.pdf
```
